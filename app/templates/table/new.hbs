{{#with (changeset this.model) as |changeset|}}
  <Form
    @config={{this.config}}
    @model={{this.model}}
    @changeset={{changeset}}
    @onSubmit={{queue (save-model changeset) (transition "table")}}
    @onCancel={{transition "table"}}
    @onAbandon={{confirm "Abandon?"}}
  />
{{/with}}