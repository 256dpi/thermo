<Form
  @config={{this.config}}
  @model={{this.model}}
  @submit={{queue (save-model this.model) (transition "table")}}
  @cancel={{transition "table"}}
  @delete={{confirm "Sure?" (queue (delete-model this.model) (transition "table"))}}
  @abandon={{confirm "Abandon?"}}
/>