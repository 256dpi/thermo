<Form
  @config={{this.config}}
  @model={{this.model}}
  @onSubmit={{queue (save-model this.model) (transition "table")}}
  @onCancel={{transition "table"}}
  @onDelete={{confirm "Sure?" (queue (delete-model this.model) (transition "table"))}}
  @onAbandon={{confirm "Abandon?"}}
/>