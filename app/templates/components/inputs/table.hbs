<div class="form__row">
  <div class="form__head">
    <label class="form__head__label">
      {{this.label}}
    </label>

    <div class="form__head__button" {{action "add"}} role="button">
      {{this.add}}
    </div>
  </div>

  <table class="form__table">
    <thead>
      <tr class="form__table__row">
        {{#each this.columns as |column|}}
          <th class="form__table__head">
            {{column.name}}
          </th>
        {{/each}}

        <th class="form__table__head" width="1"></th>
      </tr>
    </thead>

    <tbody>
      {{#each this.value as |var index|}}
        <tr class="form__table__row">
          {{#each this.columns as |column|}}
            <td class="form__table__cell">
              {{input class="form__table__input" value=(get var column.field) placeholder=column.placeholder}}
            </td>
          {{/each}}

          <td class="form__table__cell" width="1">
            <button class="form__table__button" type="button" {{action "remove" index}}>
              ✖
            </button>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{#if this.hint}}
    <div class="form__hint">
      {{this.hint}}
    </div>
  {{/if}}
</div>