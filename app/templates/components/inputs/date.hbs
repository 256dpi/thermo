{{#if @label}}
  <label class="input__label">
    {{@label}}
  </label>
{{/if}}

<EmberFlatpickr class="input__date"
                placeholder={{@placeholder}}
                @date={{or @value this.now}}
                @onChange={{fn (mut @value)}}
                @enableTime={{true}}
                @time_24hr={{true}}
                @allowInput={{not @disabled}}/>

{{#if @hint}}
  <div class="input__hint">
    {{@hint}}
  </div>
{{/if}}