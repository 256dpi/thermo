<EmberFlatpickr
  id={{@id}}
  class="input__date"
  placeholder={{@placeholder}}
  @date={{or @value this.now}}
  @onChange={{this.changed}}
  @enableTime={{true}}
  @time_24hr={{true}}
  @allowInput={{not @disabled}}
/>