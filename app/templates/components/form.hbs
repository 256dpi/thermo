{{#each config.fields as |field|}}
  {{#if (eq field.kind "value")}}
    {{#if (eq field.type "string")}}
      {{inputs/string
              label=field.title
              value=(get model field.name)
              placeholder=field.placeholder
              redacted=field.redacted}}
    {{/if}}
  {{/if}}
{{/each}}