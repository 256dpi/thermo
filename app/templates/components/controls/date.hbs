<EmberFlatpickr
  id={{@id}}
  class="input__date"
  placeholder={{@placeholder}}
  @date={{or this.value this.now}}
  @onChange={{this.changed}}
  @enableTime={{true}}
  @time_24hr={{true}}
  @allowInput={{not @disabled}}
  @disabled={{@disabled}}
/>