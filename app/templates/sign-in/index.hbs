<div class="dialog">
  <h1>
    {{this.blueprint.title}}
  </h1>

  <form class="form" {{on "submit" this.authenticate}}>
    <div class="form__row">
      <Inputs::String
        @label="Email"
        @value={{this.email}}
        @changed={{fn (mut this.email)}}
        @placeholder="user@example.org"
      />
    </div>

    <div class="form__row">
      <Inputs::String
        @label="Password"
        @value={{this.password}}
        @changed={{fn (mut this.password)}}
        @redacted={{true}}
        @placeholder="****"
      />
    </div>

    <div class="form__row">
      <button class="form__button" type="submit">
        Sign In
      </button>
    </div>
  </form>
</div>